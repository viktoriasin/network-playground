syntax = "proto3";

package ru.sinvic.server;

option java_multiple_files = true;

message MessageObject {
  int32 number = 1;
  sint64 longNumber = 2;
  bool isRandomNumber = 3;
  string text = 4;
}

message Empty {}

service MessageService {
  rpc GetMessage (Empty) returns (MessageObject);
}